{
  "name": "prismic-vue",
  "version": "0.0.0",
  "description": "Prismic package for Vue.js",
  "author": "Prismic",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": ">= 6.0.0",
    "npm": ">= 3.0.0"
  }
}
